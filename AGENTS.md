# Pflegehelfer repository instructions

Read and follow [`agent.md`](agent.md), the active [`docs/PRODUCT_CONTRACT.md`](docs/PRODUCT_CONTRACT.md), the single canonical [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md), [`docs/PRODUCT_EXPERIENCE.md`](docs/PRODUCT_EXPERIENCE.md) and [`docs/WORKFLOWS.md`](docs/WORKFLOWS.md) before changing code.

Non-negotiable: one persistent role-aware GenUI/chat/voice clinical coworker. **Conversational outside, structured inside:** staff never sees proposal/schema terminology or a form wizard; the assistant responds naturally first and shows only the minimum useful review UI. Do not add workflow-owning dashboards, focus modes or mobile module tabs. Models may interpret/compose bounded presentation; deterministic server code alone validates, authorizes and executes. Preserve negation, uncertainty, historical/occurrence time, partial/deferred work, interruption and corrections; never infer an unstated clinical action, completion or billable activity.

The PostgreSQL workday, its handover-ID/version-bound patient acknowledgements, actor-owned episodes and explicit receiving-shift transfer receipt are the only production handover/responsibility authority. Completed episodes and transferred shifts are final; one planned responsibility cannot be duplicated. Do not reintroduce seeded or provider-document handover state as an executable fallback. Generic team-role addressing must stay a role queue until a named recipient claims it; never select the first user in a list. High-assurance measurements are not current clinical facts until an independent authorized reviewer countersigns them.

Model context and recent conversation must remain bound to the currently authorized patient and captured context revision. Every entry path, including direct typed endpoints, must independently enforce the same medication/treatment boundary, task-completion evidence rules, observation time/range/high-assurance policy and provider-write protection; a UI route or model classification is never the safety boundary.
