# Alpenblick WiCare instance

This named provider instance maps to the closed WiCare adapter type. Its separate site routing does not create a new adapter or imply a verified vendor API. Production operations remain external gates until approved artifacts and tests are supplied.
