# SAP administration instance

This instance maps to the implemented SAP/vitals adapter type for approved backend services, never browser automation. Endpoint, credentials, business schema and ownership rules are secret or deployment configuration, not Markdown. Missing institution metadata keeps each affected operation gated without disabling verified independent capabilities.
