# Human approval

Clinical content is first a draft. Approval requires an authorised relationship, expected version, two patient identifiers, and explicit confirmation that the structured change was reviewed. High-assurance/four-eyes records require a second distinct identity; the author cannot provide the independent second approval. Provider acknowledgement is a separate visible state. AI output uses the same path and never bypasses it.
