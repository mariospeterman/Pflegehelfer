# Implementation status

Last updated: 2026-09-09

`DONE (SYNTHETIC)` means implemented and tested only with fictional data. `PARTIAL` is an internally achievable gap. `EXTERNAL_VENDOR_GATE` is reserved for evidence that cannot be invented in this repository.

| Area                            | Status               | Evidence / remaining limit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Canonical product direction     | DONE                 | `AGENTS.md`, `agent.md`, product contract, architecture, experience, workflows and ADRs agree on one GenUI-first coworker and one operational responsibility authority. Superseded mutable handover seed/API code is removed.                                                                                                                                                                                                                                                                                                                  |
| GenUI-first shell               | DONE (SYNTHETIC)     | One persistent conversation, mobile drawer, explicit patient bar, role opening, full fictional-data notice, responsive themes, deterministic review, safe one-tap query chips and expert Medplum link. The blue geometric Edelweiss/negative-space-plus mark avoids protected Red Cross/Crescent emblems.                                                                                                                                                                                                                                      |
| OpenUI transport/composition    | PARTIAL              | Real NDJSON transport, non-executable partial frames, strict catalog and final server authority work. Hydrated composition is intentionally deterministic today; genuine model-composed bounded presentation and cancellation durability remain internal work and are not faked.                                                                                                                                                                                                                                                               |
| Working sessions/workday        | DONE (SYNTHETIC)     | PostgreSQL/test-double sessions cover six-patient, handover-version/actor-bound acknowledgement, plan, patient/encounter-bound active/paused episodes, autosaved drafts, interruption, evidence completion, explicit defer and exact receiving-shift receipt. Terminal episodes/transferred shifts are final; planned responsibility cannot be duplicated. Legacy shared demo rosters are marked non-executable and require reset/reconciliation. Workflow Studio remains separate partial work.                                               |
| Operational PostgreSQL          | PARTIAL              | Sessions, threads/context, work episodes/segments/evidence, responsibility transfers, audit rows, domain events and hashed one-use intent/voice authority are durable and restart tested. Full intent binding still lacks first-class workflow/policy/proposal/read-set fields; provider workers consume the service checkpoint outbox instead of leasing relational rows; one cross-store accepted-command transaction is not complete.                                                                                                       |
| Conversation/context            | PARTIAL              | Turns are sequenced in PostgreSQL; patient change increments context revision and invalidates stale executable/voice authority. Responses retain the patient/revision captured at request start, bounded recent user+assistant memory is patient-filtered, and the PWA locks interactions until the server confirms context. Safe revision of a prior pending proposal is not yet durable; retention sweeper, replica-safe event replay and proactive thread insertion remain.                                                                 |
| Clinical action safety          | PARTIAL              | Generic `AssistantProposal` separates facts, performed work, observations, task changes, communications, workflow actions, ambiguity and evidence. Negation, history, uncertainty, correction, multiple observations, partial work, interruption, padded medication wording and no-action cases are regression tested. Generic tasks reject ambiguous administration verbs. High-risk observations remain non-current until a separately qualified reviewer countersigns. Formal held-out Swiss clinical-language evaluation remains required. |
| Medplum FHIR workspace          | PARTIAL              | FHIR projections, Provenance/AuditEvent, restart behavior and deliberate failing-transaction/no-partial-write verification work against Medplum 5.1.37. IDs/tags/cleanup and control artifacts are site scoped; unscoped checkpoint migration is default-denied and site+digest bound. Runtime reconstruction still depends on an HMAC-protected whole-state checkpoint Binary, so resource-scoped reconstruction and replica-safe bootstrap remain P1.                                                                                        |
| Provider-neutral hub/simulators | PARTIAL              | Per-operation manifests, destination routing, task/communication/note/observation commands, stateful provider read-back, append-only synthetic cursor updates, reset, retry/conflict/receipt behavior and automatic processing are tested. Production WiCare/careCoach/SAP/device/nurse-call operations stay individually gated. Relational leased workers/inbound durable mapping remain internal P1.                                                                                                                                         |
| Real vendor APIs                | EXTERNAL_VENDOR_GATE | Exact private contracts, authentication, sandbox credentials, mappings, error semantics and read-back acceptance evidence are unavailable; no endpoint or payload is fabricated.                                                                                                                                                                                                                                                                                                                                                               |
| Site/role configuration         | PARTIAL              | `PFH_SITE_PACK_PATH` loads a strict runtime pack that can narrow but never expand central role ceilings and assigns independent-review qualifications. Alpenblick has different shifts, provider routes, workflow language/version and patient assignments; 2/12 sizing also validates. Machine-readable rules execute while Markdown explains. Multi-department configuration, governed publishing/activation UI, alternate-site E2E and institutional sign-off remain.                                                                       |
| Team collaboration              | DONE (SYNTHETIC)     | Authorized treatment-team members share patient threads; `#patient`/role/named addressing is visible, role queues are never silently assigned to the first user, action ownership remains narrow, and sender/responder close rules are tested. A richer multi-comment history is enhancement work, not a separate application.                                                                                                                                                                                                                 |
| Local AI/knowledge              | PARTIAL              | Local OpenAI-compatible production mode, synthetic-only hosted Responses mode with `store:false`, deterministic fallback, patient-bounded multi-turn context and governed knowledge work. `pfhctl model test` performs a real non-writing synthetic structuring call. Production fast/deep packs require immutable digests and local endpoints fail closed. No key/runtime was available for this audit, and safe cross-turn proposal revision plus validated production weights remain incomplete.                                            |
| Voice                           | PARTIAL              | Local/hosted ASR adapters plus synthetic browser speech capture have MIME/size/duration limits, transcript review and no raw-audio retention. Server ASR receipts are durable/context-bound; production local ASR requires an immutable model digest and blocks redirects. No key/local ASR is installed in the current demo, so browser speech is explicitly an unverified synthetic fallback and cannot count as clinical ASR validation.                                                                                                    |
| Security/privacy/operations     | PARTIAL              | Deny-by-default policy, production demo-header rejection, durable hashed authority, safe errors, audit verification and security/ops/air-gap checks exist. PostgreSQL RLS/least privilege, one atomic cross-store command, production identity/device claims, encrypted PITR restore evidence and environment approvals remain blockers.                                                                                                                                                                                                       |

## Current assessment

The running result is the intended conversational clinical coworker, not the retired dashboard. A staff member can receive the six-patient handover, explicitly choose context, work through planned or alarm/spontaneous episodes, document several facts naturally, review only the proposed changes, reuse approved documentation in the active episode, communicate with the team, defer/resume responsibility, transfer to the next shift and observe Medplum/provider synchronization. Better models improve language understanding and bounded presentation but cannot replace or bypass policy, typed execution, Medplum, provider adapters, approvals or audit.

It is **not production-ready for real patient data or clinical use**. Remaining internal P1 work is: one accepted-command transaction spanning operational mutation/authority/audit/FHIR/provider outbox; complete workflow-step/policy/proposal/read-set intent binding; immutable handover clinical-delta/resource-version evidence (the current hash freezes roster identity only); relational leased provider workers/inbound processing and durable replica-safe SSE; Medplum reconstruction without the checkpoint Binary; executable workflow-step progression/publishing; tenant RLS/least privilege, retention sweepers and multi-replica safety; and formal clinical/model evaluation. External gates are institutional OIDC/device identity, private vendor contracts, validated Swiss-German model/ASR artifacts, deployment security/privacy/clinical approvals and production backup/restore evidence.

## Completion evidence (2026-09-09)

- Pinned Node 24 typecheck and production build pass on the current source.
- Unit/integration suite: 274 passed and 11 environment-gated tests skipped; the real PostgreSQL operational suite separately passed 11/11 after the idempotent migration.
- Manual real-browser checks on the restarted Medplum-backed PWA: 390 px, 768 px and 1440 px have no horizontal overflow or visible/console errors. The browser completed patient handover acknowledgement, explicit patient selection, natural-language review/approval, explicit negation/no-action, and a mismatched spoken patient name that failed closed into a conscious context picker. Internal planner/model identifiers no longer leak into staff review cards.
- Medplum readiness reports server `5.1.37-82e609c`, transaction atomicity verified and seven fictional Patient/Encounter resources. The Compose liveness probe, Medplum UI, local PWA, public ngrok home and public `/ready` all return healthy/HTTP 200. A durable demo reset retains accepted AuditEvent/Provenance evidence and no longer exceeds Medplum's transaction-entry limit.
- Full format/lint/typecheck/build passed. Playwright passed 33 journeys across 360 px, 390 px, tablet, 1024 px and 1440 px; 12 duplicate state-transition journeys were deliberately desktop-only and skipped on other viewports. Security, ops, synthetic restore checksum and air-gap preflight passed.
- Clean-export audit of implementation commit `8ab53da`: a fresh lockfile-strict install, generated mode-0600 demo environment, full 274-test verification/build, security/ops/air-gap checks and the complete 33-pass multi-viewport browser matrix all passed outside the development worktree at `/tmp/pflegehelfer-final-audit.xaVyR9`.
- External model/ASR calls were not made because no developer key or validated local runtime is configured. This is an explicit limit, not a hidden fallback claim.

The repository must not claim 100% production completion until every internal `PARTIAL` and required deployment acceptance gate is closed.
