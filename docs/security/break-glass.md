# Break-glass

Emergency access is a separately approved identity flow, never a query toggle. It requires strong re-authentication, reason, bounded duration, patient scope, visible banner, immutable audit, immediate security/clinical notification, and post-event review. The synthetic demo policy recognises an emergency purpose for tests but does not implement a production break-glass identity ceremony; that remains an institutional gate.
